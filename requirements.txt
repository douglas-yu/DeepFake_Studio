
# ══════════════════════════════════════════════════════════════
#  DeepFake Studio v1.0 — requirements.txt
#  Install: pip install -r requirements.txt
#  Python 3.10 / 3.11 / 3.12 recommended
# ══════════════════════════════════════════════════════════════

# ── GUI ───────────────────────────────────────────────────────
PyQt5>=5.15.0

# ── Core vision ───────────────────────────────────────────────
opencv-python-headless>=4.8.0
numpy>=2.0.0                  # Python 3.13 requires numpy 2.x; patches applied at runtime

# ── InsightFace backend ───────────────────────────────────────
insightface==1.0.1
onnxruntime>=1.19.2           # CPU; swap for onnxruntime-gpu for CUDA
onnx>=1.14.0
albumentations==1.3.1         # newer versions break insightface

# ── Torch backends (SimSwap / SadTalker / FOMM) ───────────────
# CPU-only (default). For GPU replace with:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0
torchvision>=0.15.0

# ── SimSwap extras ────────────────────────────────────────────
moviepy==1.0.3                # moviepy 2.x breaks SimSwap
imageio>=2.28.0
imageio-ffmpeg>=0.4.9
scikit-image>=0.21.0

# ── SadTalker extras ──────────────────────────────────────────
safetensors>=0.4.0
face-alignment>=1.3.5
scipy>=1.10.0
kornia>=0.7.0
einops>=0.7.0
librosa>=0.10.0
numba>=0.57.0
pydub>=0.25.0
yacs>=0.1.8
dlib>=19.24.0                 # pre-built wheel: pip install dlib
imageio[ffmpeg]

# ── GFPGAN face enhancement (optional) ───────────────────────
# Requires basicsr — install from GitHub for Python 3.13 compatibility:
#   pip install "git+https://github.com/XPixelGroup/BasicSR.git" --no-build-isolation
#   pip install gfpgan
gfpgan>=1.3.8

# ── FOMM extras ───────────────────────────────────────────────
# No additional packages beyond torch/torchvision

# ── Utilities ─────────────────────────────────────────────────
requests>=2.31.0
tqdm>=4.65.0
PyYAML>=6.0
packaging>=23.0
ffmpeg-python>=0.2.0

# ══════════════════════════════════════════════════════════════
#  NOTES
# ══════════════════════════════════════════════════════════════
#
#  GPU setup (replace torch/onnxruntime after base install):
#    pip uninstall onnxruntime torch torchvision -y
#    pip install onnxruntime-gpu
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
#  Python 3.13 — numpy<2 cannot be installed (no wheel).
#  Keep numpy>=2.0 and rely on the runtime alias patches in main_v1.1.py.
#
#  basicsr (required by gfpgan) may fail to build from PyPI on newer
#  setuptools. Use the GitHub version:
#    pip install "git+https://github.com/XPixelGroup/BasicSR.git" --no-build-isolation
#
#  albumentations must stay at 1.3.1 — newer versions break insightface.
#
#  moviepy must stay at 1.0.3 — moviepy 2.x removed the editor API
#  that SimSwap depends on.
